# ArtyrkaUpload

**ArtyrkaUpload** - это современный сервис для загрузки и обмена файлами, включающий в себя Backend API, Frontend интерфейс и Telegram-бота.

## 🚀 Возможности

- 📤 **Загрузка файлов**: Поддержка различных форматов (документы, фото, аудио, видео).
- 🔗 **Короткие ссылки**: Генерация удобных ссылок для скачивания.
- ⏳ **Срок хранения**: Возможность установки времени жизни файла (1 день, 7 дней, 1 месяц, бессрочно).
- 🤖 **Telegram Бот**: Удобная загрузка файлов прямо из Telegram.
- 💻 **Современный UI**: Адаптивный веб-интерфейс на React.

## 🛠 Технологический стек

### Backend
- **Язык**: Python 3.9+
- **Фреймворк**: FastAPI
- **База данных**: SQLite (SQLAlchemy)
- **Асинхронность**: Full AsyncIO

### Frontend
- **Фреймворк**: React 18
- **Сборщик**: Vite
- **Стилизация**: TailwindCSS + Lucide Icons
- **HTTP Клиент**: Axios

### Telegram Bot
- **Библиотека**: Aiogram 3.x
- **Клиент**: Aiohttp

---

## 📦 Установка и запуск

### 1. Предварительная настройка

Клонируйте репозиторий:
```bash
git clone https://github.com/ВашЮзернейм/ArtyrkaUpload.git
cd ArtyrkaUpload
```

Создайте файл `.env` в корне проекта и добавьте следующие переменные:
```env
BOT_TOKEN=ваш_токен_от_botfather
API_URL=http://127.0.0.1:8000
```

### 2. Запуск Backend (Сервер)

Рекомендуется использовать виртуальное окружение:

```bash
# Создание venv
python -m venv venv

# Активация (Windows)
venv\Scripts\activate

# Активация (Linux/macOS)
source venv/bin/activate
```

Установите зависимости:
```bash
pip install -r requirements.txt
```

Запустите сервер:
```bash
uvicorn main:app --reload
```
Сервер будет доступен по адресу: `http://127.0.0.1:8000`

### 3. Запуск Frontend (Веб-интерфейс)

Откройте новый терминал и перейдите в папку `frontend`:
```bash
cd frontend
```

Установите зависимости:
```bash
npm install
```

Запустите режим разработки:
```bash
npm run dev
```
Интерфейс будет доступен по адресу, указанному в консоли (обычно `http://localhost:5173`).

### 4. Запуск Telegram Бота

Откройте еще один терминал, активируйте виртуальное окружение (если используете) и запустите бота из корня проекта:
```bash
python bot.py
```

## 📝 Использование

1. **Через Сайт**: Откройте веб-интерфейс, перетащите файл или выберите его. Выберите срок хранения и нажмите "Загрузить". Скопируйте полученную ссылку.
2. **Через Бота**: Нажмите `/start` в боте, отправьте ему файл. Выберите срок хранения кнопками. Бот пришлет ссылку на скачивание.

## 📂 Структура проекта

- `main.py` - Точка входа Backend приложения.
- `bot.py` - Логика Telegram бота.
- `routers/` - Маршруты API (загрузка, скачивание).
- `schemas.py`, `models.py` - Схемы Pydantic и модели БД.
- `frontend/` - Исходный код React приложения.
- `uploads/` - Директория для хранения загруженных файлов.
